{
  "_comment": "Copy this file to test.secrets.json (gitignored) and fill in values for your test tenant. The nightly Entra ID crawler tests load credentials from this file (or from the matching environment variables — see below). When neither source is available the Entra tests are skipped with a clear message.",

  "_envVarOverrides": {
    "graph.tenantId":     "TEST_GRAPH_TENANT_ID",
    "graph.clientId":     "TEST_GRAPH_CLIENT_ID",
    "graph.clientSecret": "TEST_GRAPH_CLIENT_SECRET"
  },

  "graph": {
    "tenantId":     "00000000-0000-0000-0000-000000000000",
    "clientId":     "00000000-0000-0000-0000-000000000000",
    "clientSecret": "your-app-registration-secret"
  }
}
