{
  "_comment": "Copy this file to test.secrets.json (gitignored) and fill in values. The nightly test suite reads credentials from here (or from the matching environment variables — see below). Missing sections cause the relevant phase to skip with a clear message instead of failing.",

  "_envVarOverrides": {
    "graph.tenantId":         "TEST_GRAPH_TENANT_ID",
    "graph.clientId":         "TEST_GRAPH_CLIENT_ID",
    "graph.clientSecret":     "TEST_GRAPH_CLIENT_SECRET",
    "llm.apiKey":             "TEST_LLM_API_KEY",
    "riskProfileTestDomain":  "TEST_RISK_PROFILE_DOMAIN"
  },

  "graph": {
    "tenantId":     "00000000-0000-0000-0000-000000000000",
    "clientId":     "00000000-0000-0000-0000-000000000000",
    "clientSecret": "your-app-registration-secret"
  },

  "_llm_comment": "LLM credentials used by Phase 4j (full real-LLM risk scoring flow). The nightly pre-flight POSTs these to /api/admin/llm/config so the test can call the real provider. Provider is one of: anthropic, openai, azure-openai. For anthropic + openai, only apiKey + optional model are needed. For azure-openai, also set endpoint + deployment. Default to a smaller/cheaper model (haiku / gpt-5-mini) — a full profile + classifier generation round costs real tokens each run.",

  "llm": {
    "provider":   "anthropic",
    "model":      "claude-haiku-4-5-20251001",
    "apiKey":     "sk-ant-...",
    "endpoint":   "",
    "deployment": "",
    "apiVersion": ""
  },

  "_riskProfileTestDomain_comment": "Public domain used as the input for Phase 4j profile generation. Pick something the LLM will actually know about — a well-known company with a public presence. The domain is NOT scraped; only its name is passed to the LLM as context.",

  "riskProfileTestDomain": "portofrotterdam.com"
}
